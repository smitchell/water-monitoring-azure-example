
azure:
  storage:
    connection-string: ***
    account-name: rivermonitorstorage
    account-key: ***

spring:
  jms:
    servicebus:
      connection-string: ***
      idle-timeout: 1800000
      topic-client-id: riverobservationstopic
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=MySQL;DB_CLOSE_ON_EXIT=FALSE
